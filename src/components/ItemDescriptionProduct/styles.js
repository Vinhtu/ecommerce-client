import { makeStyles } from '@mui/styles';
import colors from '../../lib/colors';
export default makeStyles((theme) => ({
  container_item_description_product: {
    marginBottom: 24,
  },
  img_description_product: {
    width: '50%',
    height: 350,
  },
}));
